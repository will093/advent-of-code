A small framework for writing and executing solutions to advent of code problems.

To run all solutions:

`cargo run`

To run a specific year:

`cargo run yyyy`

To run a specific day:

`cargo run yyyy dd`

Inputs are stored at `/inputs/yyyy/dd.txt`

So far solutions have been written for the following years

* 2025